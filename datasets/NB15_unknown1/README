Data files:
- raw_test_for_all: raw data
- test_for_all: one-hot encoded data
- small.csv: a small version of the dataset to check for errors in data processing files
- [Anomaly type]_0.02_60.csv - file with 2% of labeled anomalies of that type with 60 of them labeled (rest unlabeled)

Python files:
- process_all_data.py: one-hot encode categorical features
- separate_data.py: separates the data from anomlies (anomaly.csv) and normal (normal.csv) data
- create_anomaly_files.py: creates the [Anomaly type]_{}_{}.csv files

Bash files:
- remove_data: to remove all the processed data files, run `source remove_data`

Usage: to process the data, run the python files in the order listed without any command prompt arguments

